# 核心依赖
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# 推理引擎
# vLLM 现在作为独立服务运行，插件通过 OpenAI-compatible API 调用
# 用户需要单独安装 vLLM（用于启动服务）：
#   pip install vllm>=0.6.0
# 插件只需要 openai 库（轻量级，无需 CUDA）
openai>=1.0.0

# 向量数据库
qdrant-client>=1.7.0
chromadb>=0.4.0

# 关系型数据库
psycopg2-binary>=2.9.0

# 嵌入模型和重排序
sentence-transformers>=2.3.0
transformers>=4.36.0
torch>=2.1.0

# 评估框架
ragas>=0.1.0

# 工具库
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
numpy>=1.24.0
typing-extensions>=4.8.0

# 异步支持
aiohttp>=3.9.0
# asyncio 是 Python 标准库，不需要安装

# 日志和监控
loguru>=0.7.0

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
